model_id: bert_tiny
model_name_or_path: prajjwal1/bert-tiny
model_type: bert
num_labels: 2
trust_remote_code: false
notes: >
  Small BERT checkpoint for fast pipeline validation. For meaningful SST-2
  accuracy, replace model_name_or_path with a fine-tuned classification
  checkpoint that is compatible with AutoModelForSequenceClassification.
